---
description: 'Wire up the devtools quick-tunnel + launcher PWA flow for real-phone preview — patches vite.config.ts, adds dev:phone script, and pre-caches the cloudflared binary.'
---

Load the `setup-phone-preview` skill.
