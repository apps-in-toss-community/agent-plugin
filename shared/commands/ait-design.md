---
description: 'Bridge a Figma design into an Apps in Toss mini-app — maps it to UX constraints (safe-area, swipe-back, PageHeader, TDS) and produces the registration image assets that /ait register consumes. Consumes an external Figma MCP if configured; degrades to manual input otherwise.'
argument-hint: '[figma-url]'
---

Load the `design` skill.
