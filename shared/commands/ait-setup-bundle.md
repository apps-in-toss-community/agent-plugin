---
description: 'Wire up the native Apps in Toss bundle build (.ait) into the current project — installs @apps-in-toss/cli, generates granite.config.ts, adds bundle:ait script, and updates .gitignore.'
---

Run the telemetry prelude described in `shared/TELEMETRY.md`, then load
the `setup-bundle` skill.
