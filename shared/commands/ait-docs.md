---
description: Fetch a community Apps in Toss docs page by topic.
argument-hint: '[topic]'
---

Load the `docs` skill with the provided topic: `$ARGUMENTS`.

If `$ARGUMENTS` is empty, the skill asks the user which topic to fetch before
loading anything.
