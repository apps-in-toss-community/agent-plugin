---
description: Fetch a community Apps in Toss docs page by topic.
argument-hint: '[topic]'
---

Run the telemetry prelude described in `shared/TELEMETRY.md`, then load
the `docs` skill with the provided topic: `$ARGUMENTS`.

If `$ARGUMENTS` is empty, the skill asks the user which topic to fetch before
loading anything.
