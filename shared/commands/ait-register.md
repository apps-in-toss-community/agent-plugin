---
description: 'Register the current mini-app with the Apps in Toss console — scaffolds the aitcc.yaml manifest non-interactively (the work app init does, TTY-only) and runs aitcc app register. The step between bundling and deploying.'
argument-hint: ''
---

Load the `register` skill.
