---
name: debug
description: |
  Analyze live browser state using the `devtools` debugging MCP when
  available; otherwise fall back to a manual guide for the DevTools panel.
  Triggered by `/ait debug`.
---

# debug skill (stub)

> TODO: implement when `devtools` debugging MCP is released. Blocked by:
> `devtools` (MCP server, not just the npm package).

See CLAUDE.md "Skills" table for the intended responsibility. Graceful
degrade: if MCP is absent, guide the user through the floating DevTools
panel manually.
