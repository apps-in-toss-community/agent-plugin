---
name: auth-setup
description: |
  Wire up `oidc-bridge` into the current project — adds Supabase/Firebase
  auth config and Toss OIDC client settings. Triggered by `/ait auth-setup`.
---

# auth-setup skill (stub)

> TODO: implement when `oidc-bridge` reaches M1 (public instance serving
> Firebase Custom Token). Blocked by: `oidc-bridge`.

See CLAUDE.md "Skills" table for the intended responsibility.
