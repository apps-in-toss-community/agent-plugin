---
name: deploy
description: |
  Deploy the current mini-app to Apps in Toss via `aitcc deploy`.
  Verifies login, runs the CLI, and interprets the result. Triggered by
  `/ait deploy`.
---

# deploy skill (stub)

> TODO: implement when `console-cli` `login` + `aitcc deploy` commands are
> complete. Blocked by: `console-cli`.

See CLAUDE.md "Skills" table for the intended responsibility.
