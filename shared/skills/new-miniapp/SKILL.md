---
name: new-miniapp
description: |
  Scaffold a new Apps in Toss mini-app from a template (React+Vite by
  default), injecting `@ait-co/devtools` as a dev dependency. Triggered by
  `/ait new`.
---

# new-miniapp skill (stub)

> TODO: implement when `devtools` 0.1.x is published to npm and
> `shared/templates/react-vite/` exists. Blocked by: `devtools`, `templates/`.

See CLAUDE.md "Skills" table for the intended responsibility.
